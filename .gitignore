# Xcode
build/
DerivedData/
*.xcuserdata
*.xcworkspace
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved


# Claude
.claude
.superpowers/

# Docs
# GitHub Pages is published from docs/, so site assets in that directory must be tracked.

# Postmark
# The real rule file never leaves the user's machine. Only the .template ships.
PostmarkRules.json
Postmark/Resources/PostmarkRules.json
# Local app-state (cooldown log, digests)
Application Support/
# Per-developer release config; never commit credentials or identities
Makefile.local
*.env
